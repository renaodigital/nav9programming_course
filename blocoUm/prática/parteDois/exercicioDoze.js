// 12 - Um banco vai liberar linhas de crédito só para servidores públicos. Caso o(a)
// servidor(a) for professor(a) terá uma linha de crédito especial com uma taxa de
// juros menor. Crie um algoritmo que possui três valores de entradas: nome
// completo (string), servidor (boolean), professor (boolean) e que imprime uma
// mensagem dizendo se a pessoa tem acesso a linha de crédito, se sim, qual linha
// de crédito.

// Valores de entrada
// Nome completo (string)
// Servidor (boolean)
// Professor (boolean)

// Mensagens
// Tem acesso a linha de acredíto, se sim dizer qual

// What's string (example)
let greeting = "Hello, ";
let name = "John";

let message = greeting + name;
console.log(message); // Output: Hello, John

// What's boolean (example)
let isTrue = true;
let isFalse = false;

if (isTrue) {
  console.log("This statement is true.");
} else {
  console.log("This statement is false.");
}


// Exercício

let nomeCompleto = "Marcos Almeida";
let professor = true;
let naoProfessor = false;
let servidor = true; 
let naoServidor = false;

if (professor, servidor) {
    console.log(name + " possui linha de crédito");
}

if (naoProfessor, naoServidor) {
    console.log (name + " não possui linha de crédito.");
}

if (professor, naoServidor) {
    console.log (name + "não possui linha de crédito.");
}

if (naoProfessor, servidor) {
    console.log (name + "pessoa não possui linha de crédito");
}